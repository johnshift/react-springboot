import { Box } from "@mui/material";

const NotFoundPage = () => {
  return (
    <Box
      sx={{
        display: "grid",
        placeItems: "center",
        height: "100vh",
      }}
    >
      <h1>Page not found</h1>
    </Box>
  );
};

export default NotFoundPage;
