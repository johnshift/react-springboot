import { h } from "preact";
import Center from "../common/Center";
import MenuSvg from "../svg/MenuSvg";

const Menu = () => {
  return (
    <div class="">
      <Center>
        <MenuSvg />
      </Center>
    </div>
  );
};

export default Menu;
