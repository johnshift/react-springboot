import { h } from 'preact';
const PageLoader = () => {
  return (
    <div id="pgld">
      {/* replace this as logo / spinner */}
      {/* <h1>{text}</h1> */}
      <div />
    </div>
  );
};

export default PageLoader;
